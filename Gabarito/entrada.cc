#include <string>
#include <iostream.h>
#include <teste>

char a, b[12], c;
double c;

void f( int a, char t[] );

void foo(int y[5], int x)
{
  char y[9];

  cout   << y;
}

int main ()
{
  int a;

  temp = b("32");
  temp = temp && 1;
  b[9] = temp;
  b(1,2,3,temp);
  a = ! temp;
  if( a ) goto i10;
  cin >> a; 
  cout << "Teste";
i10: return 0;
}
