#include <iostream>
#include <cstdio>
#include <cstdlib>
#include <cstring>

using namespace std;

int a;
char str[256];
void  soma(int a, char bambam_retorno_string[256] ) {
  cout << "Função soma()";
  cout << endl;
  strncpy(bambam_retorno_string,  "x", 256);
  return;


}

int main() { 
char ts_1[256];
  soma( 1, ts_1 );
  cout << soma;
  cout << " vqefef";
  return 0;
}

